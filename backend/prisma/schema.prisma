generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./database.db"
}

model Person {
    id              String @id @default(uuid())
    name            String
    degree          String
    birth_year      number
    email           String
    phone           number
    insurace_number number
    insurance_id    String
    insurance       InsuranceCopmany @relation(fields: [insurance_id], references: [id])
    address_id      String
    address         Address[]
    password        String
}

model InsuranceCompany {
    id      String @id @default(uuid())
    number  number
    name    String
    persons Person[]
}

model Address {
    id              String @id @default(uuid())
    country         String
    City            String
    PostalCode      Number
    street          String
    persons         Person[]
}
